import { Text, View } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

const Home = () => {
    return (
        <SafeAreaView>
        <Text style={{color: 'black'}}>Tesing tesint </Text>
        </SafeAreaView>
    )
}

export default Home